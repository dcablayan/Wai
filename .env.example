# TideFormer environment variables
# Copy to .env and fill in values. Never commit .env to git.

# NOAA CO-OPS API — no key required for public products
# (water_level, predictions, datums, metadata)
# See: https://api.tidesandcurrents.noaa.gov/api/prod/

# Optional: default NOAA station for the dashboard
# TIDEFORMER_DEFAULT_STATION=9414290

# Optional: report output directory override
# TIDEFORMER_REPORTS_DIR=./reports
